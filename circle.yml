
machine:
  services:
    - docker

dependencies:
  pre:
    - sudo apt-get update
    - sudo apt-get install -y qemu qemu-user-static binfmt-support debootstrap --no-install-recommends

test:
  override:
    - make amd64
    - make i386
    - make arm64
    - make armhf
    - make mips
